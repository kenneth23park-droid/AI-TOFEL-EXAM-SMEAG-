# StudyGround — LOCAL mode needs only the first block (no network at runtime).
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
sqlalchemy>=2.0
pydantic>=2.6

# CLOUD mode
psycopg[binary]>=3.1      # Supabase / Postgres
anthropic>=0.40           # online_feedback / rubric node — LLM_PROVIDER 에 anthropic 이 포함될 때
openai>=1.40              # 같은 노드의 병행 프로바이더 — 한쪽만 설치돼 있어도 동작한다

# Scoring pipeline — optional: the same nodes run sequentially when absent.
langgraph>=0.2

# 테스트 전용 — tests/test_speaker_images.py 의 F0 측정(ffmpeg 도 필요). 런타임에는 안 쓴다.
numpy>=1.26
