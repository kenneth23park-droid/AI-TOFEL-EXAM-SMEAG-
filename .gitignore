# macOS
.DS_Store
._*

# Word 임시 파일
~$*.docx

# extract_set1.py 가 만드는 중간 산출물 (재생성 가능)
tools/_clean_set1.txt

# 에디터
.vscode/
.idea/
*.swp

# ── 대용량 원본 자산 ───────────────────────────────────────────────
# GitHub 은 100MB 초과 파일을 거부한다. 원본은 로컬/별도 스토리지에 둔다.
TOEFL MOCK TEST SCREEN RECORDING.mp4
studyground/desktop/release/

# ── 파이썬 가상환경 · 캐시 ────────────────────────────────────────
.venv/
.venv-tts/
__pycache__/
*.pyc

# ── 로컬 DB (seed 로 재생성) ──────────────────────────────────────
studyground/data/*.db

# ── TTS 중간 산출물 (tools/tts_google.py · tools/tts_kokoro.py 가 재생성) ──
studyground/sg2/media/tts/_segments_google/
studyground/sg2/media/tts/_segments_kokoro/

# Kokoro 로 교체하기 전의 Google TTS mp3 (롤백용 로컬 사본)
studyground/sg2/media/tts/_backup_google/

# ── 의존성 (package.json 으로 재설치) ─────────────────────────────
node_modules/
studyground/sg2/media/tts/_backup_kokoro/
studyground/sg2/media/audio/_set9_backup_say/

# Claude Code 개인 설정 (옵시디언 볼트 경로 등 로컬 전용)
.claude/settings.local.json

# ── Supabase 백업 (tools/backup_supabase.py 가 받는 실데이터 · 학생 개인정보) ──
studyground/backups/
